# SUPERSEDED — there is NO Task T1 in v1.21-polish-release

This file is stale content from a prior feature and does NOT apply to v1.21.0
"The Polish Release" (feature_id `v1.21-polish-release`).

**FR-1 (the subscription count-edit revert bug) is FOLDED INTO Task T3**
(FR-3 + FR-4). T3's subscriptions rearchitect moves settings into an in-shell
bottom-sheet and switches the live poll to a targeted in-place status-text update,
which structurally eliminates the poll-clobber race. FR-1's acceptance criteria
(AC1–AC5) are satisfied within T3. See `.state/inbox/software-developer-T3.md`.

**Do NOT run this task.** The active tasks this round are T2, T3, T4, T5, T6, T7,
T8, T9 — see `.state/feature-state.json` and the exec plan's `## Task breakdown`.
