# v1.72.0 - the first-class parity wave

Spec for the two-reviewer gate. Headline = tech-debt #94: hearting a
podcast episode surfaces it in the GLOBAL Liked playlist. Scope = Dean's
intake rulings (2026-08-03, verbatim below) over the machine-derived
audit that follows. The contract this wave satisfies is the
ten-capability "first-class media experience" definition in
docs/CONTRIBUTING.md.

## Dean's intake rulings (2026-08-03, overnight run pre-authorized)

1. FULL SCOPE - build all machine-confirmed gaps for music, books AND
   podcasts, one branch, one release. Safety valve: a disproportionate
   gap gets a tech-debt row with honest sizing, never holds the wave.
2. NOTHING NEW - the ytdlp/video implementation is the template; where
   a pattern exists, port it exactly. Cells needing a product ruling
   are NOT built overnight - tech-debt row + a numbered morning
   question.
3. Videos home Continue row - BUILD (join the existing row pattern).
4. Videos manual mark-as-watched - BUILD (watch page action bar).
5. Podcast show pinnable in the Playlists surface - BUILD.
6. Tech-debt #91 - BUILD: consult autoplayNext for CROSS-KIND queue
   advances only; same-kind advances stay unconditional.
7. Books not-applicable rulings: queue (cap 3), background play (cap
   9), full player (cap 10) are NON-GOALS for books. Every other
   capability is real for books.
8. (a) Any gate seat reaching round 3: STOP, hold the branch, report
   in the morning. (b) Full release ceremony pre-authorized once both
   seats APPROVE and dual-Node suites are green.
9. The Liked playlist becomes MIXED-KIND: default view stays exactly
   as today (list/tile toggle), every kind presented the way videos
   are.
10. CONTRIBUTING's definition gets formalized post-wave: standings
    updated honestly, plus the future-agent note (what a NEW kind must
    deliver).

## The audit (machine-derived 2026-08-03 at d4f4e09, tools not memory)

Legend: DELIVERED / GAPPED / N-A (ruled) / VACUOUS. Every cell carries
its evidence. "Absent" cells were established by grep over the seam
files, not recollection.

### Capability 1 - a place

- Music: DELIVERED. `/music` (server.js:6627); content-gated sidebar
  injection via /api/music/config (public/js/common.js:3223-3227).
- Books: DELIVERED. Injection via /api/books/config
  (public/js/common.js:3208-3215).
- Podcasts: DELIVERED. Injection via /api/podcasts/health
  (public/js/common.js:3236-3240).

### Capability 2 - bottom-bar presence

- Music: GAPPED. BOTTOM_NAV_OPTIONAL = ['playlists', 'history',
  'subscriptions', 'oneoff-download', 'theme', 'podcasts']
  (public/js/common.js:3275) - no 'music'; no data-nav="music" item in
  any shell (grep over public/*.html).
- Books: GAPPED. Same evidence - no 'books' entry, no shell item.
- Podcasts: DELIVERED. In the optional list and default-hidden
  (common.js:3275, 3279); shell items present (e.g.
  public/index.html, public/music.html - data-nav="podcasts").

### Capability 3 - the one queue

- Music: GAPPED. Queue kinds are 'media' | 'podcast' ONLY
  (lib/queue/store.js:8, :56); zero addToQueue call sites in
  public/js/music.js (grep -c = 0); music rides its own browsed-order
  client context instead (public/js/common.js:1608-1616).
- Books: N-A (ruling 7).
- Podcasts: DELIVERED. kind 'podcast' (lib/queue/store.js:56), add
  site (public/js/podcasts.js:482), queueEntryHref arm
  (public/js/common.js:2762), entry_kind column
  (lib/db/sqlite.js:532-534).

### Capability 4 - the global Liked playlist

- Music: GAPPED (lane only). Per-user carriers + /api/music/liked
  routes (server.js:7288-7301), filter=liked lane (server.js:7236) -
  absent from /api/liked and the sidebar count.
- Books: GAPPED (no likes AT ALL). Zero like affordance/carrier hits
  across public/js/books.js, public/js/read.js, lib/books/*.
- Podcasts: GAPPED (lane only) = #94, THE HEADLINE. Carriers
  (lib/auth/store.js:167-170), lane route
  (lib/podcasts/index.js:880-901) - absent from /api/liked
  (server.js:9459 family) and fetchLikedTotal
  (public/js/common.js:7458).
- Reference plumbing the build widens: /api/liked collection endpoint
  (public/js/main.js:719), count-gated sidebar entry
  (common.js:7458-7495), ?liked=1 scope (main.js:494-508).

### Capability 5 - resume + the home Continue row

- Music: DELIVERED. user_music_progress (lib/auth/store.js:134);
  "Continue listening" home row (public/js/main.js:1784-1791).
- Books: DELIVERED. user_book_progress (lib/auth/store.js:96);
  "Continue reading" row (main.js:1806-1810).
- Podcasts: DELIVERED. user_podcast_progress (lib/auth/store.js:158);
  home row (main.js:1792-1805).
- VIDEOS (reference cell, ruled BUILD): progress + watching filter
  exist, but NO home Continue row - main.js's bare-home rows are
  music/podcasts/books hosts only (main.js:1778-1810).

### Capability 6 - played/consumed latch (auto + manual)

- Music: GAPPED, NEEDS-RULING (morning question M2). No played
  carrier of any kind in lib/auth/store.js (grep); no consumption
  page to host a manual toggle - the port target is not obvious, so
  per ruling 2 it is not built overnight.
- Books: GAPPED, BUILD (ruling 7 lists the latch as real for books).
  No finished carrier (grep over lib/auth/store.js). Port: manual
  mark-finished on the READ page action area, mirroring the manual
  toggle videos gain this same wave; no auto threshold (a text
  position's "end" is format-dependent - disclosed, not guessed).
- Podcasts: DELIVERED. Auto+manual: user_podcast_played
  (lib/auth/store.js:160-164), POST /api/podcasts/episodes/:id/played
  (lib/podcasts/index.js:1168).
- VIDEOS (reference cell, ruled BUILD): auto latch at the progress
  threshold (server.js:8844-8846); NO manual mark-as-watched route
  (grep over app.post/app.put) - un-watch exists only as history-row
  delete (server.js:9590).

### Capability 7 - save to device

- Music: GAPPED. /track/:id has no ?download=1 arm
  (server.js:7358-7374); no client affordance (grep over music.js).
- Books: GAPPED (half-delivered, divergent). Server arm exists -
  /book/:id/file?download=1 (server.js:6411-6419) - but hand-rolls
  the header instead of contentDispositionAttachment (non-ASCII
  titles produce percent-encoded garbage filenames), and NO client
  affordance exists (grep over books.js/read.js).
- Podcasts: DELIVERED. /episode/:id?download=1 through the shared
  helper (lib/podcasts/index.js:1003-1005); client save link
  (public/js/podcasts.js:489-498).

### Capability 8 - recoverable delete

- Music: VACUOUS. No destructive verb exists (route enumeration:
  the only music DELETE is /api/music/liked/:id, server.js:7299) -
  the every-delete-is-recoverable law holds trivially. Whether music
  SHOULD gain a delete verb = morning question M3.
- Books: VACUOUS, same evidence shape (only /api/books/pins/:id,
  server.js:6600). Morning question M3.
- Podcasts: DELIVERED. v1.70 trash lane (lib/podcasts/index.js:1074
  delete, :1120 restore; lib/podcasts/store.js:31-32 lifecycle).

### Capability 9 - background play

- Music: DELIVERED. 'music' in shouldDockOnTransition
  (public/js/player.js:135-136); v1.35 audio posture.
- Books: N-A (ruling 7). (TTS narration already docks - 'read' in the
  same list, player.js:134 - kept as-is, no work.)
- Podcasts: DELIVERED. 'podcasts' in the same list (player.js:135).

### Capability 10 - the full player

- Music: DELIVERED. type:'audio' full mount (public/js/music.js:581);
  dock tap returns via readerHref='/music'
  (public/js/player.js:5936-5943).
- Books: N-A (ruling 7).
- Podcasts: DELIVERED. Full mount (public/js/podcasts.js:605), deep
  link /podcasts?play= (public/js/common.js:2762).

### The Playlists surface (no capability cell - Dean's flagged ruling)

Pins today are channel + book only: fetchAllPins merges pinSource
'channel' + 'books' (public/js/common.js:6049-6052); routes
/api/subscriptions/pins + /api/books/pins (server.js:6381-6611).
Podcast show pin = ruled BUILD (third pinSource). A MUSIC pin
(album/artist) has no ruling - morning question M4, not built.

### Tech-debt #91 (ruled BUILD)

The autoAdvanceViaTrackNav branch advances into the queue
unconditionally (public/js/player.js:3528-3549,
advanceIntoQueueEntry) while the video path consults autoplayNext
(player.js:3554). Fix per the tracker's shape: consult the setting
for CROSS-KIND advances only (podcast -> media), same-kind stays
unconditional.

## Build plan (small task commits, each with tests, each green)

- T1 this plan (docs).
- T2 SERVER: mixed-kind global Liked - /api/liked returns liked
  videos + podcast episodes + music tracks (+ books via T6) as
  kind-carried items ({kind:'media'|'podcast'|'track'|'book'}),
  fetchLikedTotal's backing total counts all spaces; like-state
  routes stay per-kind (the existing carriers are the truth, no new
  tables). Kind carried explicitly on every item, never inferred
  from id shape; each id space keeps its own silent-drop scoping;
  same-id-both-kinds collision tests with both rows live.
- T3 CLIENT: the Liked view renders kind-aware cards in BOTH list and
  tile modes (art route, title line, href per kind:
  media -> /watch.html?v=, podcast -> /podcasts?play=,
  track -> /music (dock-play seam), book -> /read.html), presented
  exactly like video cards otherwise (ruling 9).
- T4 videos home Continue row (port the music/podcasts/books row
  chassis; deep-link watch.html?v= at saved position; row toggle key
  ft-home-continue-videos in the same Settings editor family).
- T5 videos manual mark-as-watched: POST/DELETE /api/watched/:id
  (new per-user route family = second real session + wrong-user
  assertions at the ROUTE layer in the birth commit), watch page
  action-bar toggle; history/watched filter read the same latch.
- T6 books: like affordance (read page action area + books joins
  T2's global Liked), manual mark-finished latch (new id-keyed
  carrier, ALL arms in the birth commit per the carrier law),
  save-to-device client affordance + the server header moves to
  contentDispositionAttachment.
- T7 music: save-to-device (?download=1 arm on /track/:id + song-row
  affordance), music joins T2's global Liked.
- T8 music + books join the bottom bar: BOTTOM_NAV_OPTIONAL +
  default-hidden (the podcasts v1.71 posture), items in every shell.
- T9 music joins the one queue: entry_kind 'track', kind-dispatched
  resolution + queueEntryHref arm + panel art, advance in/out
  correct, silent-drop scoped per id space (the v1.71 widening is
  the template; carrier scoping + collision tests).
- T10 #91 cross-kind autoplayNext consult (player.js seam above).
- T11 podcast show pins: pinSource 'podcasts', routes
  (/api/podcasts/pins CRUD + reorder), sheet + pinned-sidebar arms.
- T12 CONTRIBUTING standings update + future-agent note; ROADMAP;
  tech-debt rows (M1-M4 below); release ceremony.

Morning questions (NOT built - ruling 2): M1 books auto-finish
threshold (manual-only shipped), M2 music played latch, M3 delete
verbs for music/books (cap 8 is vacuous today), M4 music pin in the
Playlists surface.

## Gate brief seeds

Full gate, never slim (per-user data). Adversarial attack surfaces:
the kind-confusion seam everywhere an id crosses a table (liked,
queue, pins), the same-id-both-kinds collision (both rows LIVE at
the destructive moment - the v1.71 W3 vacuity lesson), wrong-user
probes on every new route family (v1.71 W4), the Liked view's
mixed-kind pagination/count coherence, silent-drop scoping per id
space, and the trash/purge carriers of the new books-finished and
liked arms.
