# v1.48 - metadata + UX wave (EXEC PLAN, in progress)

**Status:** intake answered by Dean; items 1, 2, 3, 5 IMPLEMENTED + committed on
`feature/v1.48-metadata-ux-wave`. Items 4 and 6 outstanding. Gate not yet run.

This file started as a handoff brief and is now the working exec plan. It
doubles as the two-reviewer gate's spec.

---

## Dean's six items + intake outcome

| # | Item | Status |
| - | ---- | ------ |
| 1 | Full video descriptions | DONE - commit `b9b20e8` |
| 2 | Real day-of view counts, re-heatable | DONE - commit `5b96780` |
| 3 | Related files 10 -> 20 | DONE - commit `4a280a7` |
| 4 | Modernise old mock commenters | OUTSTANDING - diagnosed, see below |
| 5 | Appearance to top of Library settings | DONE - commit `18d1973` |
| 6 | Duplicate file after a one-off download | BLOCKED on Dean's directory listing |

Intake decisions Dean confirmed:

- Wave ships as **v1.48.0**; RBAC moves to v1.49.
- View-count wording: **"when downloaded"** (his pick).
- The yt-dlp **403 is dropped from this wave** - Dean: "Let's ignore for now I
  think we're okay it's not happening." Do NOT retry `--js-runtimes node`.
- Related 10 -> 20 accepted along with its padding trade-off (see item 3).

---

## Item 1 - full descriptions (DONE)

**The brief's assumption was wrong and this is worth remembering: the
truncation was RENDER-ONLY. No reheat or backfill work was needed or done.**

Verified end to end that the full text was always present:

- `--embed-metadata` is passed unconditionally (`lib/ytdlp/args.js:1148`).
- ffprobe reads format tags under `maxBuffer: 16MB` (`server.js`), a ceiling
  whose own comment says it was raised specifically for long descriptions.
- `parseFfprobeTags` whitelists `description` and only trims whitespace.

Two client defects produced the symptom:

1. The description was never rendered in the description box at all.
   `#description-paragraph` is static boilerplate ("File Path: ... This file is
   self-hosted on your network..."). The description appeared only as one row of
   the "Embedded info" block, cut to 400 chars by `renderEmbeddedTags`'s `clip`.
2. `-webkit-line-clamp` lived on that boilerplate, so "Show more" expanded three
   lines of static text and never any description.

Fix: the description is its own element (`#video-description`) carrying
`.description-text`, so the existing clamp/expand mechanism now governs it. The
boilerplate moved to an unclamped `.description-fileinfo`. `description` is
skipped by `renderEmbeddedTags` (it would otherwise print twice, once clipped).
The 400-char clip survives for every OTHER tag, where it still guards layout.

`resolveDisplayDescription` (pure, exported, 10 unit tests) owns the display
decision. Its title-equality guard is load-bearing: for universal-lane items the
title IS the description (`%(title).100s`, v1.41.16-18), so without it those
items print the same text twice. The guard is equality, deliberately NOT prefix.

## Item 2 - real view counts (DONE)

**The single most important fact: the field is `sourceViewCount`, never
`viewCount`.** `item.viewCount` was already taken - the legacy pre-v1.42 LOCAL
watch counter, still honored as a floor by `effectiveViewCount`. Using that name
would have made a fresh download report ~12M local plays on the stats page.

Second trap: yt-dlp emits `view_count` as a JSON NUMBER, and run.js's `present()`
helper only passes non-empty STRINGS. Routing it through `present()` would have
nulled every count - dead on arrival with a green suite (the v1.47.4 class).
`presentNumeric` handles it and preserves a genuine `0`.

Flow: both `--print` selectors -> `presentNumeric` -> `store.parseCapturedViewCount`
(non-negative integer; rejects floats/NaN/Infinity/negatives; 1e15 ceiling) ->
persisted WITH its capture date as a unit.

Carriers updated in the same commit (v1.42 lesson): changed-file re-init
carry-forward, and BOTH halves of the Phase-2 merge (completed-mid-scan adoption
+ partial-reheat gap-fill). Also joined run.js's "anything usable" gate and
index.js's `hasAnythingToPersist` chain. Move re-key and backup/restore already
carry it (whole-item move, wholesale namespace).

Reheat SUPERSEDES (re-snapshotting is the feature) and is network-only, so a
reheat that cannot reach the network never destroys a stored count.

Render: watch page "1.2M views when downloaded"; cards bare. No capture -> mock
count retained (a library of "0 views" would look broken; fake stars stay).

## Item 3 - related 10 -> 20 (DONE)

`RESULT_COUNT` 10 -> 20. Both length tests re-typed the literal `10` rather than
reading the exported constant, so they pinned a COPY of the cap; they now assert
against `RESULT_COUNT`, with one test deliberately pinning the value. The
padding fixture built 11 candidates against a cap of 10 and would have decayed
into "returned everything there was" at 20 - it now builds `RESULT_COUNT + 1`.

Disclosed trade-off: `rankRelated` concatenates similar + recent and slices, so
the extra slots are filled from the most-recent tail on small libraries.

## Item 4 - modernise old mock commenters (OUTSTANDING, diagnosed)

**It is not a code bug.** A line-joined whole-tree scan found ZERO real names
left in source (only `ROADMAP.md` describing the v1.44.3 incident, and
`package.json`'s legitimate `"author": "Dean Tammam"`). The current
`MOCK_COMMENT_BANK` is fully genericised.

The names live in **Dean's browser localStorage**. `watch.js`'s `loadComments`
writes generated comments to `comments_${mediaId}` on a video's FIRST view and
never regenerates:

```js
const localComments = localStorage.getItem(savedCommentsKey);
if (localComments) { comments = JSON.parse(localComments); }
else { comments = getMockInitialComments(); localStorage.setItem(...); }
```

So every video opened before v1.44.3 has Daisy/Ray/Zak frozen client-side.
Dean confirmed the names: **Daisy Tammam, Ray Tammam, Zak Goldin.**

**THIS CAN LOSE DATA.** Dean's own posted comments live in the same array under
`author: 'You'`. Planned fix (flagged to him, awaiting objection): a migration
that drops only entries whose author is absent from the current bank, KEEPS
every `'You'` comment, and tops back up from the current bank. A blunt
version-the-key reset would wipe his real comments.

## Item 5 - Appearance to top (DONE)

Pure markup reorder in `public/setup.html`. Safe because every control is bound
by id in `setup.js`, never by document order. Verified no duplicate ids and no
test pins section order.

## Item 6 - duplicate file after a one-off (BLOCKED)

Dean's two paths, both `.mp3`, differing only by the id bracket:

```
/media/ytdlp/vvmtest/The Caretaker - Everywhere At The End Of Time - Stages 1-6 (Complete) [wJWksPWDKOc].mp3
/media/ytdlp/vvmtest/The Caretaker - Everywhere At The End Of Time - Stages 1-6 (Complete).mp3
```

It was a **YouTube** URL. He reports the bracketed copy shows under the channel
folder (expected) and the bracket-less one shows as the "ytdlp" folder - which
suggests the latter actually sits at `/media/ytdlp/` root, not in `vvmtest`.

Ruled out so far:

- `maybeRelocate` is reheat-only, not the one-off path (inherited from the brief,
  still not disproven).
- Background audio extraction writes to `audioPath(id)` - a cache dir, keyed by
  id, `.m4a`. Not a media-folder writer.
- `lib/ytdlpIntermediates.js` cannot match a bracket-LESS file: every pattern is
  anchored to the ` [<11-char id>]` bracket, deliberately (the v1.15.1 hotfix-2
  data-loss incident).
- yt-dlp's own templates always produce a bracket, so the bracket-less copy was
  NOT written by our download argv.

Leading hypothesis **(A): a pre-existing bracket-less IMPORT.**
`relocateHydratedImportIntoChannelFolder` exists precisely for these -
server.js's v1.41.5 note describes ".mp3/.mp4 files MeTube downloaded into a
normal library root, with no `[<id>]` bracket but with the source YouTube URL in
their embedded comment/purl tag". If Dean already had that Caretaker mp3, the
one-off had no way to know it was the same video: an import carries no
`.ytdlp-archive.txt` entry, so the archive dedup could not fire.

If (A) holds, the fix is **duplicate DETECTION before download** (warn/skip when
an existing item resolves to the same YouTube id), with **no deletion anywhere**.

Alternative **(B): a relocation that left its source behind** - would mean the
checksum-verified unlink (v1.41.7) failed.

**Discriminator (asked, awaiting):** `ls -la --time-style=full-iso /media/ytdlp/
/media/ytdlp/vvmtest/`. If the bracket-less file predates the download, it is (A).

**Gate posture: FULL two-reviewer gate, never slim** (destructive-work norm).
Brief the adversarial seat to try to make the fix delete the WRONG copy. Do NOT
"fix" this with a duplicate reaper - a reaper that picks the wrong copy is the
v1.41.6 file-destroyer class this repo shipped once already.

---

## Remaining work

1. Item 4 migration (data-loss-capable - preserve `'You'` comments).
2. Item 6 diagnosis + fix once Dean's listing arrives.
3. Full two-reviewer gate on the whole branch (QA seat + adversarial seat).
4. Fix round, then delta re-confirm with the SAME agents until both APPROVE.
5. Dual-Node suites (v22.23.1 AND v24.14.0).
6. Release ceremony per CLAUDE.md. Docker publish is Dean's.

## Process notes carried forward

- The gate found v1.47.4's headline fix was dead code and two CRITICALs in
  v1.47.8. Budget for REQUEST CHANGES.
- Divergent fixtures stay green through broken pipelines - build fixtures from
  real output. (Item 3's tests were a live instance: they re-typed the constant.)
- A repaired lock is not a verified lock. Mutation-test after every repair.
- False comments were the most common defect last wave. A claim in a comment is
  part of the deliverable. (One was caught and corrected mid-build this wave:
  an item-1 HTML comment claimed "inline spans" while the CSS said `display:
  block`; the design was reworked rather than the comment patched.)
