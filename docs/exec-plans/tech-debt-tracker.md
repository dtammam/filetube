# Tech Debt Tracker

## Active

| # | Description | Severity | Added | Source |
|---|-------------|----------|-------|--------|
| 2 | `sweepAgedTranscodes` and `evictTranscodeCache` each do an independent `readdir`+`statSync` over `TRANSCODE_DIR` back-to-back at both call sites (startup + post-produce), a double directory pass per produce. Minor perf; fold into a single shared enumeration if the cache-hygiene path is revisited. | Low | 2026-07-05 | settings-automation-cache review round 1 (PE remediation, non-blocking) |
| 5 | Manual `DELETE /api/videos/:id` deletes `db.metadata[id]` + `db.progress[id]` but does NOT call `clearPersistedServedAt(id)`, so a manually-deleted RECENTLY-SERVED video leaves one stale `persistedServedAt` Map entry until id-reuse or process restart. The scan-prune path DOES clear it (server.js ~1107); only the manual-delete route omits it. Bounded (one Map entry per manually-deleted recently-served id), minor, pre-existing/orthogonal to the HR2 fix. Proposed fix: add `clearPersistedServedAt(id)` inside the DELETE route's `updateDatabase` mutator, mirroring the scan-prune path. | Low | 2026-07-05 | harden-db-writes-and-logo HR2 targeted re-review (out-of-scope observation; tech-debt-tracked per the stop line) |
| 4 | HR1b (finding-D) same-path re-add drop, transient/self-healing (server.js ~1086, the Phase-2 membership drop loop). If a file is DELETEd and a NEW file is dropped at the SAME path DURING a scan (so both resolve to the same md5 id), the drop loop removes that id for ONE scan cycle — `fresh.metadata` lacks the id (the DELETE removed it) while `phase1Ids` still has it, so the re-added file is treated as a concurrent delete and dropped. Consequence: the re-added file is invisible until the NEXT scan (≤30 min with auto-scan On; a manual "Scan now" otherwise). SELF-HEALS in one scan cycle, NO permanent data loss — this is the inherent resurrect-vs-drop ambiguity of a same-path/same-id delete+re-add landing within a single scan. Proposed future fix: before dropping an id, check whether the file currently exists on disk at that path / was freshly re-scanned THIS pass (present in the current `scannedFiles`/`newMetadata` build with a live stat), and KEEP it if so rather than dropping. | Low | 2026-07-05 | harden-db-writes-and-logo HR1 focused re-review (converged tail; tech-debt-tracked, not fixed) |
| 6 | D7b (T4 fix round #2, partial): `runScanDirectories` (server.js ~938) now parses the yt-dlp config ONCE per scan and passes it into `ytdlp.extraScanRoots(config)`, but `extraScanRoots` itself still does its own `fs.existsSync(downloadDir)` (needed for the D1 disabled-with-content-survives gate) AND the generic per-folder scan loop right below it (server.js ~954) does a SECOND `fs.existsSync` on the same resolved path (needed for the mount-loss guard, uniformly, across every folder). Eliminating the second check would require `extraScanRoots` to hand back existence state alongside the path, changing its pure `config -> string[]` contract that multiple existing tests call directly — not safe to do inside a tightly-scoped fix round. Harmless (one extra stat syscall per scan cycle); revisit only if the scan path's I/O profile is ever a real bottleneck. | Low | 2026-07-06 | yt-dlp-integration-module T4 fix round #2 (T4b), D7b disposition |
| 7 | `server.js:945`'s `Array.from(new Set([...(db.folders \|\| []), ...ytdlp.extraScanRoots(ytdlpConfig)]))` only collapses BYTE-IDENTICAL string duplicates. If an operator manually adds the yt-dlp download dir to `db.folders` with a non-canonical spelling (trailing slash, a `.` segment, different case on a case-insensitive filesystem, etc.) that resolves to the same real path as `extraScanRoots`'s `path.resolve`-normalized entry, the `Set` does not collapse it and the same tree is walked twice per scan. Perf-only: the scan keys files by path in a `Map` so the tree is never double-INDEXED, just double-WALKED. A fix would mean resolving every entry before de-duping, which changes what gets persisted/displayed for manually-configured folders -- out of scope for a tightly-scoped fix round. | Low | 2026-07-06 | yt-dlp-integration-module T4 fix round #3 (T4c), E1 tail |
| 8 | D4 (round #2)'s reordering -- validating a `subId` exists BEFORE the busy-check in `runPoll` -- means every coalesced-while-busy re-pull-one trigger now does a synchronous `store.listSubscriptions` (`loadDatabase` disk read) even though a poll is already in flight, whereas before only the FIRST (non-coalesced) call paid that cost. Under a burst of triggers arriving while busy, this is a main-thread `loadDatabase`-per-trigger cost. Not a correctness issue (D4 closing the bogus-id-during-busy 404 gap was correct); a future optimization could cache the subscription id set for the duration of an in-flight poll. **RE-CONFIRMED DEFERRED at v1.29.0 T5** (the repull `onDecision` work touched this surface): the in-flight sub-id cache was evaluated and deliberately NOT implemented per the Design's only-if-clean guidance — low value on a single-user LAN box; still open. | Low | 2026-07-06 | yt-dlp-integration-module T4 fix round #3 (T4c), E1 tail (D4 restated); re-confirmed deferred v1.29.0 T5 |
| 9 | D1 custom-dir-never-enabled edge: a never-enabled install with `FILETUBE_YTDLP_DOWNLOAD_DIR` pointed at a pre-existing, populated directory (e.g. an operator sets the ENV var before ever flipping `FILETUBE_YTDLP_ENABLED` on, and that path happens to already contain unrelated media) would have that directory scanned/indexed by `extraScanRoots`'s `fs.existsSync` check even though the module has never been enabled -- because the directory's mere existence (not the module ever having created it) satisfies the gate. Unusual misconfiguration (the download dir is normally created empty by `ensureDownloadDir` only once enabled); low likelihood, but not structurally impossible. Not fixed: closing it would require `extraScanRoots` to distinguish "exists because we created it" from "exists because it was already there," which isn't information the pure `config -> string[]` contract currently carries. | Low | 2026-07-06 | yt-dlp-integration-module T4 fix round #3 (T4c), E1 tail (D1 restated) |
| 10 | Prune/mount-loss redesign for preserved (module-owned) content. The yt-dlp download dir is a CONDITIONALLY-RECOMPUTED scan root (`extraScanRoots(config)`, re-derived every scan from `isEnabled(config) OR fs.existsSync(downloadDir)`), not a persisted "known root" the scanner remembers across cycles independent of that gate. Consequence: prune-missing can still reap downloaded content's `db.progress` watch-positions + thumbnails/transcode sidecars (never the video files themselves, which live on disk and are simply re-indexed on the next successful scan) in forms the E1 mount-loss guard can't detect, specifically -- (1) an EMPTY-BUT-PRESENT mountpoint after an unmount (`fs.existsSync` reports `true`, `readdir` returns 0 entries, so the root never lands in `missingRoots` and every previously-indexed id under it looks genuinely gone) -- note this is a **PRE-EXISTING global prune-missing weakness affecting EVERY library folder**, not specific to the yt-dlp module; the module only makes it more likely to matter (removable/network storage is more common for a download target than for a manually-curated library folder); (2) a CHANGED `FILETUBE_YTDLP_DOWNLOAD_DIR` value orphaning the OLD resolved root -- the new config's `extraScanRoots` no longer contributes the old path at all, so it is absent from `currentFolders` entirely (never even reaches the `missingRoots` check) and its ids are pruned as if deleted; (3) DISABLED + a simultaneous transient unmount (documented separately as the E3 known limitation in `docs/ARCHITECTURE.md` and the exec plan) -- same reap, narrower trigger combination. Recommended fix (Dean's Option C, the right-depth fix): make prune-missing treat "a root's ENTIRE prior content vanished at once" as an UNMOUNT SIGNATURE in its own right (protect, don't reap) as a GENERAL prune-missing safeguard -- this benefits every configured library folder, not just the yt-dlp module, and would also close case (1) above for the whole codebase. Alternative/complementary: a module-owned PERSISTED "known root" (independent of the live `isEnabled`/`fs.existsSync` gate) fed into the mount-loss guard's expected-roots set, which would additionally close case (2). Not implemented -- deliberately deferred; E1 (T4 fix round #3) is the PARTIAL fix that closes only the "enabled + existsSync-false" case via the OR-gate. Dean's call: "ship v1.11.0 now, track the deep fix" (2026-07-06). | Medium | 2026-07-06 | yt-dlp-integration-module T4 fix round #3 cleanup pass (F4), deferred deep fix |
| 11 | Unbounded-memory tradeoff with `FILETUBE_YTDLP_MAX_VIDEOS=0` ("unlimited"). The v1.11.1 hotfix moved the LIST path (`spawnYtdlp` in `lib/ytdlp/run.js`) from `execFile`+`maxBuffer` to `spawn` with no size-based cap at all, specifically so a legitimately large channel's `--dump-json` output can never trip a hard buffer ceiling and abort the whole listing. With the default (`FILETUBE_YTDLP_MAX_VIDEOS=25`), `--playlist-end` bounds the real-world size upstream regardless. But an operator who explicitly opts into `maxVideos: 0` disables that upstream bound too, so the ENTIRE channel's `--dump-json` NDJSON output is buffered in memory at once (raw stdout Buffer chunks, `Buffer.concat`'d once at `close`) -- for a very large/prolific channel this could be a spike of hundreds of MB, all held for the duration of one listing pass. This is opt-in (not the default) and documented in code (the `spawnYtdlp` module comment) and in `README.md`'s ENV table, but is tracked here so the unbounded-memory tradeoff isn't silently reintroduced/worsened by a future change. Suggested future fix: a soft line-count or byte-size ceiling applied ONLY to the `maxVideos: 0` case (e.g. log a warning and truncate/stop reading past some very large threshold) rather than a hard `execFile`-style abort, preserving the "large channels must always list successfully" guarantee this hotfix restored while still bounding worst-case memory for the unlimited case. | Low | 2026-07-06 | fix/ytdlp-list-scope hotfix, two-reviewer gate fix round (FIX round follow-up, NOTE 3) |
| 12 (AF1) | FR-G's scan-root dedup (`normalizeScanRoot`-keyed `Set`, server.js ~955) correctly collapses two PRE-EXISTING symlink-alias `db.folders` entries that point at the same real tree into a single surviving row -- but on the collapse scan, the DROPPED duplicate row's ephemeral watch-progress (`db.progress`) and thumbnails tied to the dropped id's file paths are pruned (files themselves are safe; one row survives with its own progress). Intended dedup behavior, not a bug -- accepted per the re-review. Future nicety: merge the dropped duplicate's `db.progress` into the kept id's on collapse instead of just pruning it, so a viewer's watch position on the aliased path isn't lost. | Low | 2026-07-06 | ytdlp-metube-parity re-review (fast-follow, accepted narrow limitation) |
| 13 (AF2) | If an operator sets `FILETUBE_YTDLP_DOWNLOAD_DIR` to an EXISTING mapped library folder (a `db.folders` row), FIX-2's synthetic-exclusion (POST /api/config) drops that folder from `db.folders` on the very next config save -- it becomes display-only synthetic instead. This loses the real `db.folders` row's mount-loss protection for that folder: a later module-disable plus a transient unmount during a scan could then reap that folder's content, since the E1 OR-gate's `db.folders` membership no longer covers it. RECOMMENDATION (also added to `README.md`'s yt-dlp section): use a DEDICATED download dir, never an existing mapped library folder. Future proper fix: in `GET /api/config`, don't synthesize the download folder if its resolved path already matches a real `db.folders` entry; in `POST /api/config`, don't exclude it from `validFolders` if it was already a real (pre-save) `db.folders` row. | Medium | 2026-07-06 | ytdlp-metube-parity re-review (fast-follow, accepted narrow limitation) |
| 14 (AF3) | If `FILETUBE_YTDLP_DOWNLOAD_DIR` is an ANCESTOR directory of a mapped library folder, `cleanDisplayTitle`'s `matchRootFolder` gate can cosmetically clean a non-yt-dlp file's display title -- but only in the narrow case where that unrelated file's name coincidentally ends in an 11-character bracketed suffix matching the yt-dlp id shape. Display-only (no id/data impact; `getMediaId` is unaffected). Same "use a dedicated download dir" guidance (also in `README.md`) mitigates it, since it also resolves AF2. | Low | 2026-07-06 | ytdlp-metube-parity re-review (fast-follow, accepted narrow limitation) |
| 16 | A manually-named file `X [<colliding-11-char-id>].mp4` dropped under the yt-dlp download root can absorb an UNCONSUMED `db.ytdlp.downloadMeta` channel identity entry -- the scan bridge (`store.consumeDownloadChannelMeta`) trusts the `[<id>]` bracket suffix (preceded by a space in the filename) without correlating it back to the subscription/one-off download that actually produced that id. Bounded: scoped to the download root only, the absorbed identity is still a REAL, previously-validated channel (never attacker-injected), and it requires the unusual user action of hand-placing/renaming a file to collide with a still-pending captured id before the real download's scan consumes it. Not fixed -- would require carrying a stronger per-id correlation (e.g. a file-hash or mtime cross-check) into the bridge, out of scope for this fix round. | Low | 2026-07-07 | v1.20-subscribe two-reviewer gate (FIX A/B follow-up round) |
| 25 | v1.29.0 T0 run-log line has no per-LINE byte-size cap. Individual fields are already bounded upstream (`MAX_REASON_LENGTH`=500, `MAX_STATUS_LENGTH`=300) and the file itself is capped at `YTDLP_RUNLOG_MAX_ENTRIES`=500 lines with atomic rewrite, so a pathological single line is bounded in practice — but there is no explicit whole-line length guard. QA SUGGESTION at the two-reviewer gate; accepted as defense-in-depth only, not fixed. Future nicety: cap the serialized JSONL line length (truncate/skip) in `runlog.recordRun`. | Low | 2026-07-11 | v1.29.0 downloads-reliability two-reviewer gate (QA suggestion) |
| 26 | v1.29.0 GF3 documented ambiguity: `computeDownloadOutcome`'s reserve-at-least-one bound at `remainingAfterAttributed === 1` (with ≥1 unattributed failure) DELIBERATELY consumes the last remaining slot → `error`/`succeeded:0` (AC-FM-D intentionally wins over reserve-at-least-one). Dean-approved as the Direction-A-safe choice (consequence is one extra retry cycle with `cutoffDate` frozen, never a lost window); pinned by a boundary-lock test + the doc comment. Tracked because it is a deliberate conservative bias, not a proven-optimal one — revisit only if real per-item retry accounting is ever added. | Low | 2026-07-11 | v1.29.0 downloads-reliability GF2 focused re-gate (W1, Dean-resolved) |
| 27 | v1.29.0 T0/T4 disabled-module JSONL-absence: the run log is created lazily only from the enabled-only `recordRun` call sites and the history route lives inside the `isEnabled` gate, so with `FILETUBE_YTDLP_ENABLED` off no `ytdlp-runs.jsonl` is created and the route 404s (verified by AC2.4). QA SUGGESTION: add a dedicated regression test asserting NO jsonl file appears under `DATA_DIR` after a full disabled-module boot (beyond the route-404 test), to lock the no-op against a future `recordRun` call slipping outside the gate. Not added this round. | Low | 2026-07-11 | v1.29.0 downloads-reliability two-reviewer gate (QA suggestion) |
| 18 | `channelDir` in `GET /api/subscriptions` exposes the server's absolute filesystem path for each subscription's per-channel download directory. Consistent with this codebase's existing posture elsewhere (e.g. `root=` query params already carry absolute paths across this same LAN-facing API surface), and the client only ever uses it to build an `encodeURIComponent`-safe link (never rendered via `innerHTML`) -- but it is still an absolute-path disclosure worth tracking if this API's trust boundary ever changes (e.g. if it's ever exposed beyond a trusted LAN). Not fixed -- accepted, matches existing precedent. | Low | 2026-07-07 | v1.20-subscribe two-reviewer gate (FIX A/B follow-up round) |
| 19 | B1 per-channel re-pull button (`common.js` `probeAndReconcileRepullButton`): the `GET /api/subscriptions/health` probe latches `repullHealthChecked` only when the FIRST probe *resolves*, and — unlike the `/api/subscriptions` fetch — has no shared in-flight promise. If more than one router hook fires before that first response returns (e.g. `bootRouter` immediately followed by a fast navigation, or a slow server), each issues a fresh health GET. Bounded by the number of navigations in a ~millisecond window against an idempotent, cheap endpoint; real-world impact negligible. Fix: mirror the subscriptions in-flight dedup, or set `repullHealthChecked = true` optimistically before dispatching the probe. | Low | 2026-07-09 | v1.24.1 Wave 2 delta re-gate (adversarial review, non-blocking nit #1) |
| 20 | B1 re-pull button: `fetchSubscriptionsForRepull` caches the `/api/subscriptions` result (including an empty list on a network error, via `.catch(() => [])`) for `REPULL_SUBS_CACHE_MS` (5s). Consequence: a transient network blip can suppress the button on a valid subscribed channel for up to 5s, and a subscription added seconds ago shows no button for up to 5s. Self-heals; consistent with the design's stated caching tradeoff (chosen to prevent a fetch stampede across a burst of navigations). Fix (if ever warranted): don't cache the error result, or invalidate the cache on a known subscription mutation. | Low | 2026-07-09 | v1.24.1 Wave 2 delta re-gate (adversarial review, non-blocking nit #2) |
| 21 | B1 re-pull button: after the health probe 404-latches the module as disabled, every subsequent navigation still calls `reconcileRepullButton` → `removeRepullButton` → one `getElementById` (no fetch, no DOM write). Trivially cheap and confirmed otherwise permanently inert (test asserts `healthCalls===1`, `subsCalls===0`, zero DOM mutation across repeated calls). Fix (if ever warranted): short-circuit the disabled path before the `getElementById`. | Low | 2026-07-09 | v1.24.1 Wave 2 delta re-gate (adversarial review, non-blocking nit #3) |
| 22 | Concurrent move + delete of the SAME item id (v1.24.2 T9 move-files). `moveItemToFolder`'s FS operation is non-atomic with its own re-key `updateDatabase` mutator; both it and `DELETE /api/videos/:id` mutate through the serialized `updateDatabase`. If a DELETE commits between the move's FS operation and its mutator, the mutator sees the old id gone and returns 404 ("removed before the move could be recorded") **while the file is already physically at `newPath` with no `db.metadata` entry**. Not data loss (the next scan re-indexes the file under a fresh id), but the item's watch history is lost and the user sees a 404 on a move that physically happened. Requires two concurrent ops on ONE item — unusual on a single-user LAN box — hence Low; current behavior is regression-locked by `test/integration/move-files.test.js`. Proposed fix: on `freshItem` missing, if the file exists at `newPath`, re-index it (recreate `db.metadata[newId]` from `newPath`) instead of 404; or take a per-id in-process lock spanning the FS move + mutator. | Low | 2026-07-09 | v1.24.2 Wave 3 two-reviewer gate (adversarial #1 / QA #5a — accepted, self-heals) |
| 23 | Crash-window non-atomicity in the move pipeline (v1.24.2 T9 move-files). A process death between `moveItemToFolder`'s FS operation (`linkSync` / `copyFileSync`+`unlinkSync`) and its `updateDatabase` re-key mutator leaves the file moved while the db still points at `oldPath` → next scan prunes `oldId` (watch progress lost) and re-adds `newId` fresh. The same-device `linkSync`+`unlinkSync` path additionally leaves a SELF-HEALING dual hardlink (same inode, NOT data loss) if the crash lands between the link and the unlink — strictly safer than the pre-gate silent-clobber it replaced. Inherent to FS+JSON non-atomicity and consistent with the existing DELETE route's posture; Low. Proposed future fix: write an intent-journal entry before the FS move so a scan can reconcile a moved-but-unrecorded file (would also close #22 more cleanly). | Low | 2026-07-09 | v1.24.2 Wave 3 two-reviewer gate (adversarial #2 — accepted, documented) |
| 24 | Cancel (v1.24.7 T15 A3) SIGKILLs only the DIRECT yt-dlp child, not its process group — an `ffmpeg` grandchild mid-postprocess can be orphaned and keep running. This is the SAME posture as the pre-existing timeout-kill the design cites as precedent, but v1.24.7's subtitle grab adds `--convert-subs vtt` (which invokes ffmpeg during postprocessing), widening the window where a cancel can leave an ffmpeg orphan. Bounded: the orphan is a short-lived transcode of an already-partial file (self-terminates), single-user LAN box, and the partial media file is still reaped by `cleanupFailedDownloadIntermediates`. Not fixed: a real fix means spawning the yt-dlp child `detached` in its own process group and `kill(-pid, 'SIGKILL')` to take down the whole tree, which changes the spawn posture app-wide (also affects the timeout-kill path) — out of scope for a tightly-scoped gate fix. Proposed future fix: process-group spawn + group-kill for both cancel and timeout. | Low | 2026-07-10 | v1.24.7 Wave 5 two-reviewer gate (adversarial nit — accepted, pre-existing posture widened) |
| 29 | v1.30.0 T7 (A5 client) full-list callers cap. `watch.js` `loadRelatedFiles` + `setupPrevNext` and `player.js` `handleAutoplayNext` need the FULL ordered library/folder list (for rankRelated / deriveOrderedIds neighbor computation), so they request `/api/videos` with an explicit `limit=1000000` — which the server clamps to `lib/videoQuery.js` `MAX_LIMIT=10000`. Consequence: for a library (or a single folder) exceeding **10,000 items**, related / prev-next / autoplay-advance would SILENTLY TRUNCATE to the first 10k in sort order — an item beyond position 10k could compute a wrong/absent neighbor. Far beyond Dean's current ~1,300-item scale and above the SQLite-revisit trigger (#28, ~2,000+), so not a live bug; adversarial-review WARNING at the v1.30 two-reviewer gate, accepted (not fixed). **Revisit if:** any single library folder (or the total library, for the unscoped related list) approaches ~10,000 items — consistent with #28's scale-trigger handling. Future fix: a dedicated "ordered ids only" server endpoint (ids + sort, no per-item payload) for the neighbor/related computations, unbounded by page limit; or raise/remove MAX_LIMIT for these internal callers with a memory-aware guard. | Low | 2026-07-12 | v1.30-scale-perf-and-polish two-reviewer gate (adversarial WARNING, accepted) |
| 30 | v1.30.0 T6 (A5 server) `lib/videoQuery.js` `matchesSearch` has no explicit null-guard on `item.title` / `item.folderName` before `.toLowerCase().includes(...)`. This is **byte-for-byte preserved pre-existing behavior** — the prior inline `/api/videos` search predicate (title-or-folderName case-insensitive `includes`) had the same shape, and in practice the scan always populates both fields as strings, so it does not throw today. QA SUGGESTION at the v1.30 two-reviewer gate; deliberately NOT changed in that round for scope discipline (the extraction was a faithful mirror, not a behavior change). Future defense-in-depth: add `(item.title \|\| '')` / `(item.folderName \|\| '')` guards in `matchesSearch` so a hand-edited/partial `db.json` metadata row missing a title can never throw the search path. | Low | 2026-07-12 | v1.30-scale-perf-and-polish two-reviewer gate (QA suggestion, note only) |
| 28 | **SQLite migration DEFERRED at v1.30 Design (A6) — authorized by Dean, but the evidence did not warrant it.** The v1.30 read-only perf sweep's bottlenecks are not storage-engine problems: B1 (freeze) is an algorithmic O(N²) sidecar readdir (fixed by A1's per-scan readdir cache), B2 (unresponsive) is a synchronous walk on the event loop + a blocking `POST /api/scan` (fixed by A2's cooperative async walk + 202 ack), B3 (read storm) is best solved by an in-memory read cache since the whole db fits in memory (~800 KB at ~1300 items, ~1.2 MB projected at ~2000 — A3), and B5 (write amplification) is fully bounded by progress-write batching (A4). `better-sqlite3` is a native addon that would add Alpine/musl + Node 22 AND Node 24 ABI-matching fragility to the release gate (AC8.2 tests Node 24), plus a mandatory one-way-migration-with-backup+rollback and a re-validation of every crash-safety/preserved-guard AC against a new store — a large blast radius on a crash-safety-sensitive codebase, for a single-user box the incremental path already makes fast. **Revisit if ANY of:** (1) the FR1 heartbeat proxy still records event-loop stalls > 50 ms/stretch during an unchanged rescan at N ≥ 2000 items after A1–A5 ship; (2) `db.json` exceeds ~5 MB (≈ several thousand items) such that a single real-mutation `saveDatabase` (`JSON.stringify` + `fsync`) exceeds ~50 ms; (3) boot parse of `db.json` exceeds ~100 ms; (4) the single-writer assumption is broken (multi-user / a second writer process is added) — the in-memory-cache coherency argument no longer holds and a transactional store becomes warranted. See the v1.30 exec plan's `## Design` A6 verdict for the full rationale. | Medium | 2026-07-11 | v1.30-scale-perf-and-polish Design (A6 evidence-based deferral; silent deferral prohibited) |
<!-- Items added by agents when persistent failures or gaps are found -->

## Closed

| # | Description | Severity | Added | Closed | Resolution |
|---|-------------|----------|-------|--------|------------|
| 1 | Transcode cache (`data/transcoded/`) grew unbounded — watched AVIs accumulated (~5.6 GB pileup observed) with no eviction | High | 2026-07-04 | 2026-07-04 | Size-capped LRU eviction (`TRANSCODE_CACHE_MAX_BYTES`, default 5 GB) + startup orphan `.tmp.mp4` cleanup + recently-served protection so an actively-watched file is never evicted (feature `avi-ux-refinement`) |
| 3 | FR3.4's `MAX_RESCAN_FOLLOWUPS=1` rescan-drain bound can DROP a rescan requested during the single follow-up pass, but ONLY when auto-scan is Off (no periodic timer to self-heal). Scenario: `scanIntervalMinutes` Off; scan in flight (pass 1); a rescan is requested (pass 2 = the one allowed follow-up); a `POST /api/config` folder-add lands DURING pass 2 → sets `rescanRequested=true`, but the drain has spent its budget and exits leaving it unconsumed → folder B's media isn't indexed until a manual "Scan now". NO data loss (the folder is persisted in `db.folders`); with the timer On (30m default) the next periodic scan covers it. This is the documented trade-off of bounding FR3.4's livelock. | Low | 2026-07-05 | 2026-07-05 | `scheduleDeferredRescan()` (server.js): when the bounded drain exits with `rescanRequested` still true, arms exactly ONE deferred, rate-limited (5s), `unref()`'d re-entry into the already-bounded `scanDirectories()` instead of dropping it — single-guarded (`if (deferredRescanTimer) return`) so sustained demand never stacks more than one pending timer. Resolves the dropped-rescan without reintroducing the FR3.4 livelock risk (feature `harden-db-writes-and-logo`, v1.9.0). |
| 17 | The subscription-poll cycle's fallback recorded `sub.channelUrl` for EVERY survivor video id NOT captured via the FTCHMETA `--print` mechanism — INCLUDING survivors whose download FAILED — leaving a `db.ytdlp.downloadMeta` entry with no corresponding on-disk file (fed item #16's collision window). | Low | 2026-07-07 | 2026-07-11 | Closed by **v1.29.0 T3 (T3a)**: per-video outcome threading (`computeDownloadOutcome` + the 3-arm branch in `runSubscriptionCycle`) now records channel-meta fallback (`recordSurvivorChannelMetaFallback`) ONLY for the genuinely-succeeded ids (`survivorIds` minus the attributed-failed set); attributed-failed and all-unattributed/zero-attributed (now classified `error`) ids get no `downloadMeta` entry. Regression-locked by the outcome-threading integration test (9/10 → meta for the 9 not the 1; zero-attributed → no meta for either id). |
| 15 | `extractMetadataAndThumbnail`'s two ffmpeg art/frame-extraction calls (server.js, the audio embedded-artwork `extractArtCmd` and the video-frame `extractFrameCmd`) still build a shell-string command with `filePath`/`thumbPath` interpolated and run it via `exec()`, the same pre-existing pattern the scan's ffprobe call had before v1.18.0's FR-1b hardened THAT one line to `execFile('ffprobe', [...])`. Deferred (not fixed) this round to keep the FR-1b diff narrowly scoped to the line it was already editing; same pre-existing surface, not worsened. Future fix: convert both to `execFile('ffmpeg', [...])` with `filePath`/`thumbPath` as distinct arg-array elements, mirroring the ffprobe conversion. | Low | 2026-07-07 | 2026-07-07 | v1.19.0 two-reviewer-gate follow-up (FIX A): both spawns converted to `execFile('ffmpeg', [argArray], cb)` — the audio embedded-artwork extraction (`['-i', filePath, '-an', '-vcodec', 'copy', '-y', thumbPath]`) and the video frame-grab (`['-ss', String(timestamp), '-i', filePath, '-vframes', '1', '-q:v', '2', '-y', thumbPath]`) — so neither ever shell-interprets `filePath`/`thumbPath`, matching the ffprobe `execFile` hardening. `test/integration/scan-thumbnail-preserve.test.js`'s mocks updated to observe both via `execFile` (keyed on `bin === 'ffmpeg'`) while still meaningfully proving frame-grab invocation/non-invocation. |
