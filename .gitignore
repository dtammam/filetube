# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json

# --- Env / secrets ---
.env
.env.*
!.env.example
*.pem
*.key

# --- Logs ---
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Node / JS ---
node_modules/
dist/
build/
.next/
out/
coverage/
.cache/
.turbo/
*.tsbuildinfo

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# --- Go ---
bin/
*.exe
*.test

# --- FileTube runtime data (regenerable / personal) ---
# db.json holds folder paths + watch progress; .thumbnails/ is a regenerable cache.
# In Docker these live under the mounted DATA_DIR (./data); in local dev they land in the repo root.
/db.json
# v1.42: the SQLite store + WAL sidecars (same reasoning as db.json above).
/filetube.db
/filetube.db-wal
/filetube.db-shm
/filetube.db.tmp
/filetube.db.tmp-wal
/filetube.db.tmp-shm
.thumbnails/
/data/

# --- Uploads / user media (app data) ---
uploads/
media/
storage/
tmp/
*.sqlite
*.sqlite3
*.db

# --- Local agent/editor settings ---
.claude/settings.local.json

# Legacy handoff-harness pipeline state (retired v1.41.19)
.state/
